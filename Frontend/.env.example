# Local development uses Vite's /api proxy to FastAPI on 127.0.0.1:8000.
# In production, replace this with the deployed API URL including the router prefix.
VITE_API_BASE_URL=/api/anomaly-health-monitoring
