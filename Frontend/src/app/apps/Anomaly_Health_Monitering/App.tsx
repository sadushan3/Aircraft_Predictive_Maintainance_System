import { RouterProvider } from 'react-router-dom'

import { router } from '../../Routes/Anomaly_Health_Monitering/routes'

export default function App() {
  return <RouterProvider router={router} />
}
