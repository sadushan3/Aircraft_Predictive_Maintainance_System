# Predictive Maintenance Frontend

This folder contains the requested React + TypeScript + Vite structure for the predictive maintenance dashboard.

## Run locally

1. `cd Frontend`
2. `npm install`
3. `npm run dev`
