# ---------------------------------------------------------------------------
# Core scientific stack
# ---------------------------------------------------------------------------
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.4
joblib>=1.3
networkx>=3.2
h5py>=3.10
tqdm>=4.66

# ---------------------------------------------------------------------------
# API layer
# ---------------------------------------------------------------------------
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-multipart>=0.0.9

# ---------------------------------------------------------------------------
# Optional deep-learning extras (lazy-imported).
# The package runs end-to-end on the RandomForest / IsolationForest path
# WITHOUT these. Install them to activate the LSTM twin, LSTM autoencoder and
# Monte-Carlo-Dropout uncertainty paths.
# ---------------------------------------------------------------------------
tensorflow>=2.15        # LSTM twin, LSTM autoencoder, MC-Dropout
shap>=0.44              # exact SHAP attributions (falls back to permutation importance)
